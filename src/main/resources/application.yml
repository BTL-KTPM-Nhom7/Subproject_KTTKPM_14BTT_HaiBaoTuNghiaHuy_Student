server:
  port: 8080

spring:
  application:
    name: STUDENT-SERVICE

eureka:
  client:
    register-with-eureka:
      value: true
    fetch-registry:
      value: true;
    service-url:
      defaultZone: https://subprj-discovery.herokuapp.com/eureka